allow git -C * add *
allow git -C * commit *
allow git add *
allow git commit *
allow npm run build
allow npm run check
allow npm run format
allow npm run lint
allow npm run test *
allow npm run test:e2e:ui *
allow npm run typecheck
allow npm test
allow python3 tests/*.py *

allow rm -rf .playwright-cli
allow rm -rf test-results

allow skills/agent-comm/scripts/ac *

allow-read ~/projects/hermes-agent/**
